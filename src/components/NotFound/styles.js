import { css } from '@emotion/core';
import { flexGrow, justifyCenter } from '../../styles/misc';

export const mainContent = css`
  ${flexGrow};
  ${justifyCenter};
  padding: 16px 40px;
`;

export const homeLink = css``;
